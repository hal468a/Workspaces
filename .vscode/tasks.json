{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "source /opt/ros/galactic/setup.bash && colcon build --symlink-install && find . -wholename './src/*.py' -exec chmod +x {} \\;"
        }
    ]
}